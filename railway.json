{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd backend && pip install -r requirements_prod.txt && python manage.py migrate && python manage.py collectstatic --noinput"
  },
  "deploy": {
    "numReplicas": 1,
    "startCommand": "cd backend && gunicorn foodlion.wsgi --log-file -"
  }
}
